# R 3.0 compatibility:

importFrom("grDevices", "colorRampPalette", "grey")

importFrom("graphics", "abline", "arrows", "axis", "image", "layout",
             "legend", "lines", "mtext", "par", "points", "rect",
             "segments", "strheight", "text", "grid", "hist", "box", "boxplot")

importFrom("methods", "as", "slot")

importFrom("stats", "as.dendrogram", "as.formula", "as.hclust",
             "complete.cases", "cutree", "density", "lm", "median",
             "na.omit", "predict", "quantile", "dnorm", "sd")

importFrom("utils", "URLencode", "download.file", "read.csv", "setTxtProgressBar", "txtProgressBar")

# TODO: make these more specific
import(aqp)
import(ape)

importFrom(igraph,
  graph.edgelist,
  count.multiple,
  get.adjacency,
  graph.adjacency,
  get.edgelist,
  get.edges,
  add.edges,
  simplify,
  E,
  'E<-',
  V,
  'V<-',
  degree,
  minimum.spanning.tree,
  delete.edges,
  cluster_walktrap,
  cluster_fast_greedy,
  membership,
  layout_with_fr,
  plot_dendrogram,
  plot.igraph
)

importFrom(cluster,
  daisy,
  diana
)


importFrom(lattice,
  levelplot,
  panel.levelplot,
  panel.segments
)

importFrom(digest,
  digest
)

importFrom(latticeExtra,
  dendrogramGrob
)

importFrom(vegan,
  decostand,
  vegdist
)

importFrom(soilDB,
  format_SQL_in_statement,
  SDA_query
)

importFrom(sp,
  spsample,
  coordinates,
  over,
  SpatialPointsDataFrame,
  is.projected,
  CRS,
  spTransform
)

importFrom(reshape2,
  dcast,
  melt
)

importFrom(Hmisc, 
  hdquantile,
	wtd.mean, 
  wtd.var, 
  wtd.quantile, 
  wtd.rank, 
  wtd.table,
  binconf				 
)
	
importFrom(scales, 
  alpha,
  rescale
)

importFrom(circular, 
  circular,
  mean.circular,
  rho.circular,
  plot.circular,
  density.circular,
  axis.circular,
  arrows.circular
)

importFrom(RColorBrewer, 
  brewer.pal
)

importFrom(plyr, 
  join,
  dlply,
  ldply,
  ddply,
  summarise
)


exportPattern("^[^\\.]")
